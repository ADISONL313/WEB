<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqcskj.studentsadminvisul.mapper.ClassesMapper">

    <resultMap id="classResultMap" type="Classes">
    </resultMap>
    <select id="selectClasses" resultMap="classResultMap">
        select classes.id,classes.major_id,major.college,major.majorName,classes.className,classes.grade from major,classes <where>
        and classes.major_id=major.id <if test="majorName!='' and majorName!=null"> and majorName like CONCAT("%",#{majorName},"%") </if><if test="className!='' and className!=null">
       and className like CONCAT("%",#{className},"%")
    </if><if test="grade!='' and grade!=null">
        and grade=#{grade}
    </if>
    </where>
        order by grade,className
    </select>
</mapper>